export enum EMailTemplate {
  DEFAULT = 'default',
  RECOVER_PASSWORD = 'recover-password',
  COMPANY_ACTIVATION = 'company-activation',
  SCHEDULING_CANCEL = 'scheduling-cancel',
  SCHEDULING_CONFIRM = 'scheduling-confirm',
  SCHEDULING_COMPLETED = 'scheduling-completed',
}
